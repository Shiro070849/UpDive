/**
 * Example Constants
 * This is a sample constants file - replace with your actual constants
 */

const STATUS = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
  PENDING: 'pending'
};

const ROLES = {
  ADMIN: 'admin',
  USER: 'user',
  GUEST: 'guest'
};

module.exports = {
  STATUS,
  ROLES
};
